<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'AI Kanban Board' %></title>
    
    <!-- Tailwind CSS + DaisyUI -->
    <link rel="stylesheet" href="/css/main.css">
    
    <% if (locals.hx) { %>
        <!-- Load htmx only when needed -->
        <script src="https://unpkg.com/htmx.org@1.9.10"></script>
    <% } %>
</head>
<body>
    <header class="navbar bg-base-100 shadow-sm">
        <div class="navbar-start">
            <a href="/" class="btn btn-ghost text-xl font-bold text-primary">🎯 AI Kanban</a>
        </div>
        <div class="navbar-end">
            <% if (locals.user) { %>
                <!-- Authenticated navigation -->
                <a href="/dashboard" class="btn btn-ghost mr-2">Dashboard</a>
                <span class="text-base-content mr-4">Hello, <%= locals.user.name %></span>
                <form method="POST" action="/auth/logout" class="inline">
                    <button type="submit" class="btn btn-outline btn-sm">Sign Out</button>
                </form>
            <% } else { %>
                <!-- Guest navigation -->
                <a href="/auth/login" class="btn btn-outline mr-2">Sign In</a>
                <a href="/auth/register" class="btn btn-primary">Get Started</a>
            <% } %>
        </div>
    </header>
    
    <main class="container mx-auto px-4 py-8">
        <%- body %>
    </main>
    
    <!-- Flash messages will be added later -->
    
    <script>
        // Basic progressive enhancement
        console.log('🎯 AI Kanban Board loaded');
        <% if (locals.hx) { %>
            console.log('✨ htmx enhanced');
        <% } %>
    </script>
</body>
</html>