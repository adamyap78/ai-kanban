<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'AI Kanban Board' %></title>
    
    <!-- Tailwind CSS - will be added later -->
    <style>
        /* Minimal reset and basic styles for now */
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            line-height: 1.6;
            color: #333;
            background: #f5f6fa;
        }
        .container { max-width: 1200px; margin: 0 auto; padding: 0 1rem; }
        .btn {
            display: inline-block;
            padding: 0.5rem 1rem;
            background: #3b82f6;
            color: white;
            text-decoration: none;
            border-radius: 0.375rem;
            border: none;
            cursor: pointer;
            transition: background 0.2s;
        }
        .btn:hover { background: #2563eb; }
        .header {
            background: white;
            border-bottom: 1px solid #e5e7eb;
            padding: 1rem 0;
            margin-bottom: 2rem;
        }
        .nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .logo { font-size: 1.5rem; font-weight: bold; color: #3b82f6; }
    </style>
    
    <% if (locals.hx) { %>
        <!-- Load htmx only when needed -->
        <script src="https://unpkg.com/htmx.org@1.9.10"></script>
    <% } %>
</head>
<body>
    <header class="header">
        <nav class="nav container">
            <div class="logo">🎯 AI Kanban</div>
            <div>
                <% if (locals.user) { %>
                    <!-- Authenticated navigation -->
                    <a href="/dashboard" style="color: #6b7280; text-decoration: none; margin-right: 1rem;">Dashboard</a>
                    <span style="color: #6b7280; margin-right: 1rem;">Hello, <%= locals.user.name %></span>
                    <form method="POST" action="/auth/logout" style="display: inline;">
                        <button type="submit" class="btn" style="background: #6b7280; padding: 0.5rem 1rem; font-size: 0.875rem;">Sign Out</button>
                    </form>
                <% } else { %>
                    <!-- Guest navigation -->
                    <a href="/auth/login" class="btn" style="background: #6b7280; margin-right: 0.5rem;">Sign In</a>
                    <a href="/auth/register" class="btn" style="background: #059669;">Get Started</a>
                <% } %>
            </div>
        </nav>
    </header>
    
    <main class="container">
        <%- body %>
    </main>
    
    <!-- Flash messages will be added later -->
    
    <script>
        // Basic progressive enhancement
        console.log('🎯 AI Kanban Board loaded');
        <% if (locals.hx) { %>
            console.log('✨ htmx enhanced');
        <% } %>
    </script>
</body>
</html>