<div class="mb-8">
    <div class="flex justify-between items-center mb-4">
        <div class="flex items-center">
            <a href="/orgs/<%= organization.slug %>" class="link text-base-content/70 mr-4">← <%= organization.name %></a>
            <h1 class="text-3xl font-bold text-base-content"><%= board.name %></h1>
        </div>
        
        <div class="flex gap-2">
            <a href="/orgs/<%= organization.slug %>/boards/<%= board.id %>/settings" class="btn btn-neutral btn-sm">Board Settings</a>
            <button class="btn btn-success btn-sm" disabled>+ Add Card (Coming Soon)</button>
        </div>
    </div>
    
    <% if (board.description) { %>
        <p class="text-base-content/70 mb-6 text-lg">
            <%= board.description %>
        </p>
    <% } %>
    
    <% if (locals.flash?.success) { %>
        <div class="alert alert-success mb-4">
            <%= locals.flash.success %>
        </div>
    <% } %>
    
    <% if (locals.flash?.error) { %>
        <div class="alert alert-error mb-4">
            <%= locals.flash.error %>
        </div>
    <% } %>
</div>

<div class="card bg-base-100 shadow-xl min-h-96">
    <div class="card-body text-center">
        <div class="text-6xl mb-4">🎯</div>
        <h2 class="card-title text-2xl justify-center mb-4">Board Ready!</h2>
        <p class="text-base-content/70 mb-6 max-w-md mx-auto">
            Your board has been created with default lists: <strong>To Do</strong>, <strong>In Progress</strong>, and <strong>Done</strong>.
            The full Kanban interface is coming in Phase 2!
        </p>
        
        <div class="alert alert-info my-8 max-w-lg mx-auto text-left">
            <div>
                <h3 class="font-medium mb-3">What's Next (Phase 2):</h3>
                <ul class="list-disc ml-4 space-y-1">
                    <li>Full Kanban board with drag-and-drop cards</li>
                    <li>Add, edit, and delete cards</li>
                    <li>Move cards between lists</li>
                    <li>Card details with descriptions and due dates</li>
                    <li>Labels and assignees</li>
                </ul>
            </div>
        </div>
        
        <div class="alert alert-warning my-6 max-w-lg mx-auto">
            <div>
                <p class="text-sm">
                    <strong>Development Note:</strong> This board is fully functional but the UI is still being built. 
                    All your data is safely stored in the database.
                </p>
            </div>
        </div>
        
        <div class="card-actions justify-center mt-8">
            <a href="/orgs/<%= organization.slug %>" class="btn btn-neutral">Back to Organization</a>
            <a href="/orgs/<%= organization.slug %>/boards/<%= board.id %>/settings" class="btn btn-primary">Board Settings</a>
        </div>
    </div>
</div>

<div class="card bg-base-200 mt-8">
    <div class="card-body">
        <h3 class="card-title text-lg mb-4">Board Information</h3>
        <div class="grid gap-4 grid-cols-1 md:grid-cols-3">
            <div>
                <div class="text-sm text-base-content/70 mb-1">Created</div>
                <div class="font-medium"><%= new Date(board.createdAt).toLocaleDateString() %></div>
            </div>
            <div>
                <div class="text-sm text-base-content/70 mb-1">Last Updated</div>
                <div class="font-medium"><%= new Date(board.updatedAt).toLocaleDateString() %></div>
            </div>
            <div>
                <div class="text-sm text-base-content/70 mb-1">Organization</div>
                <div class="font-medium"><%= organization.name %></div>
            </div>
        </div>
    </div>
</div>